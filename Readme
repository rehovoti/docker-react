Travis CI - continuos integration provider
AWS Hosting - Amazon web services
npx create-react-app frontend
npm run test
npm run build
npm run start
docker build -f Dockerfile.dev .
docker run -it -p 3000:3000 <CONTAINER_ID>

Volumes in docker -refs to actual dirs
docker run -it -p 3000:3000 -v /app/node_modules -v $(pwd):/app <IMAGE_ID>
docker run -it -p 3500:3000 -v /app/node_modules -v $(PWD):/app -e CHOKIDAR_USEPOLLING=true b678dc6fc46b

attach to running container
sudo docker exec -it 894795cb5f6f npm run test